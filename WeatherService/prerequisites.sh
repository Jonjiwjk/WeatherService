#!/bin/bash

# Install Python
# No specific installation is needed as Python comes pre-installed in many operating systems. If Python is not installed, you can use the package manager of your operating system to install it.

# Install Flask
python -m pip install flask

# Install the Requests library
python -m pip install requests